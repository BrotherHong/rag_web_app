# ==============================================================================
# 核心框架 (Core Framework)
# ==============================================================================
# FastAPI - 現代化的高效能 Web 框架
fastapi>=0.115.0,<0.116.0

# SlowAPI - Rate limiting
slowapi>=0.1.9

# Uvicorn - ASGI 伺服器 (standard 包含 uvloop, httptools 等效能優化)
uvicorn[standard]>=0.30.0,<0.31.0

# Python-Multipart - 處理檔案上傳
python-multipart>=0.0.9,<0.1.0

# ==============================================================================
# 資料庫與 ORM (Database & ORM)
# ==============================================================================
# SQLAlchemy - 強大的 ORM 框架 (2.0+ 支援非同步)
sqlalchemy>=2.0.35,<3.0.0

# AsyncPG - PostgreSQL 非同步驅動
asyncpg>=0.29.0,<0.30.0

# Alembic - 資料庫遷移工具
alembic>=1.13.0,<2.0.0

# ==============================================================================
# 快取 (Caching) - 可選
# ==============================================================================
# Redis - 快取與 Session 管理 (可選功能)
redis>=5.0.0,<6.0.0

# Celery - 任務佇列
celery>=5.4.0,<6.0.0

# Flower - Celery 監控介面
flower>=2.0.1,<3.0.0

# ==============================================================================
# 認證與安全 (Authentication & Security)
# ==============================================================================
# Python-JOSE - JWT Token 處理
python-jose[cryptography]>=3.3.0,<4.0.0

# Passlib - 密碼雜湊與驗證
passlib[bcrypt]>=1.7.4,<2.0.0

# Bcrypt - 直接指定 bcrypt 版本以確保相容性
bcrypt>=4.0.1,<5.0.0

# ==============================================================================
# 資料驗證 (Data Validation)
# ==============================================================================
# Pydantic - 資料驗證與序列化
pydantic>=2.9.0,<3.0.0

# Pydantic-Settings - 設定管理
pydantic-settings>=2.5.0,<3.0.0

# Email-Validator - Email 格式驗證
email-validator>=2.2.0,<3.0.0

# Google Auth - 驗證 Google ID Token
google-auth>=2.36.0,<3.0.0

# ==============================================================================
# 檔案處理 (File Handling)
# ==============================================================================
# AIOFiles - 非同步檔案 I/O
aiofiles>=24.1.0,<25.0.0

# 文件轉換工具
markitdown[all]>=0.1.5,<0.2.0
python-docx>=1.1.0,<2.0.0
openpyxl>=3.1.0
opencv-python-headless>=4.11.0.86
mineru[core]==2.7.6

# OpenCC - 簡繁轉換
opencc-python-reimplemented>=0.1.7

# Requests - HTTP 請求（保留用於舊代碼）
requests>=2.31.0

# HTTPX - 異步 HTTP 客戶端（用於 Ollama API）
httpx>=0.27.0

# LiteLLM - 統一 LLM API 介面與負載均衡
litellm==1.81.16

# NumPy - 數值計算
numpy>=2.4.0,<3.0.0

# tqdm - 進度條
tqdm>=4.66.0

# ==============================================================================
# RAG 功能 (RAG Functionality)
# ==============================================================================
# PyTorch - 固定 2.9.1（pypi.org CUDA 12 版本，uv.lock 確認可用，cu130 需要 driver 580+）
torch==2.9.1

# Sentence Transformers - Reranker 模型
sentence-transformers>=5.2.0,<6.0.0

# OpenCC - 簡繁轉換（已包含但再次確認）
opencc-python-reimplemented>=0.1.7

# ==============================================================================
# 環境變數管理 (Environment Management)
# ==============================================================================
# Python-Dotenv - .env 檔案讀取
python-dotenv>=1.0.0,<2.0.0

# ==============================================================================
# 測試工具 (Testing)
# ==============================================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0

# ==============================================================================
# 版本要求
# ==============================================================================
# Python 3.10+ (使用 SQLAlchemy 2.0 的 async 功能、新型別提示等)
# 建議使用 Python 3.10 或更高版本
